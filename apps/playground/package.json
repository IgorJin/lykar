{
  "name": "@lykar/playground",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "npm run build --workspace @lykar/editor-bridge",
    "dev": "node server.mjs",
    "build": "npm run build --workspace @lykar/editor-bridge",
    "typecheck": "node --check server.mjs && node --check public/playground.js && node --check scripts/seed.mjs && node --check scripts/smoke.mjs"
  },
  "dependencies": {
    "pg": "^8.16.0"
  }
}
