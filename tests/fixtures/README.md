# Fixtures

Static and server-rendered pages used to verify target resolution and operation
replay.

`fixtures/sdk-consumer/index.html` is the script-only SDK installation fixture;
it does not import workspace source or a neighboring package.
